diff --git a/Cargo.toml b/Cargo.toml
index 50c3cf7..7900c4f 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -93,6 +93,7 @@ members = [
   "src/options_types",
   "src/output",
   "src/runtime",
+  "src/poly_python",
   "src/simdutf_sys",
   "src/which",
   "src/transpiler",
@@ -453,6 +454,7 @@ bun_options_types = { path = "src/options_types" }
 bun_bunfig = { path = "src/bunfig" }
 bun_output = { path = "src/output" }
 bun_runtime = { path = "src/runtime", default-features = false }
+poly_python = { path = "src/poly_python" }
 bun_simdutf_sys = { path = "src/simdutf_sys" }
 bun_which = { path = "src/which" }
 bun_transpiler = { path = "src/transpiler" }
diff --git a/src/bun_bin/Cargo.toml b/src/bun_bin/Cargo.toml
index 94839e8..91d0ffb 100644
--- a/src/bun_bin/Cargo.toml
+++ b/src/bun_bin/Cargo.toml
@@ -34,4 +34,5 @@ bun_platform.workspace = true
 # Phase-C: bun_runtime now compiles green; pulled in for `Cli::start()`.
 # bun_jsc remains gated until the JSC bridge links.
 bun_runtime.workspace = true
+poly_python.workspace = true
 # bun_jsc.workspace = true
diff --git a/src/bun_bin/lib.rs b/src/bun_bin/lib.rs
index ba12490..d76d844 100644
--- a/src/bun_bin/lib.rs
+++ b/src/bun_bin/lib.rs
@@ -200,6 +200,10 @@ pub(crate) unsafe extern "C" fn main(argc: c_int, argv: *const *const c_char) ->
     output::stdio::init();
     let _flush = output::flush_guard();
 
+    if let Some(exit_code) = run_python_entrypoint() {
+        return exit_code;
+    }
+
     // 5. Per-thread stack-limit cache for the JS recursion guard.
     StackCheck::configure_thread();
     bun_io::ParentDeathWatchdog::install();
@@ -209,3 +213,28 @@ pub(crate) unsafe extern "C" fn main(argc: c_int, argv: *const *const c_char) ->
     // `Global::exit` is `-> !`; it coerces to the `c_int` return type.
     Global::exit(0)
 }
+
+fn run_python_entrypoint() -> Option<c_int> {
+    let mut args = std::env::args_os().skip(1);
+    let first = args.next()?;
+    let (entry, script_args) = if first == "run" {
+        (args.next()?, args)
+    } else {
+        (first, args)
+    };
+    let path = std::path::PathBuf::from(entry);
+    if path.extension().and_then(|extension| extension.to_str()) != Some("py") {
+        return None;
+    }
+
+    let script_args: Vec<String> = script_args
+        .map(|argument| argument.to_string_lossy().into_owned())
+        .collect();
+    Some(match poly_python::run_file(&path, &script_args) {
+        Ok(exit_code) => exit_code as c_int,
+        Err(error) => {
+            eprintln!("poly: {error}");
+            1
+        }
+    })
+}
diff --git a/src/jsc/bindings/BunObject+exports.h b/src/jsc/bindings/BunObject+exports.h
index 47a1f8d..e6da7af 100644
--- a/src/jsc/bindings/BunObject+exports.h
+++ b/src/jsc/bindings/BunObject+exports.h
@@ -65,6 +65,7 @@
     macro(mmap) \
     macro(nanoseconds) \
     macro(openInEditor) \
+    macro(polyPythonCall) \
     macro(registerMacro) \
     macro(resolve) \
     macro(resolveSync) \
diff --git a/src/jsc/bindings/BunObject.cpp b/src/jsc/bindings/BunObject.cpp
index 696460b..61edf18 100644
--- a/src/jsc/bindings/BunObject.cpp
+++ b/src/jsc/bindings/BunObject.cpp
@@ -985,6 +985,7 @@ JSC_DEFINE_HOST_FUNCTION(functionFileURLToPath, (JSC::JSGlobalObject * globalObj
     mmap                                           BunObject_callback_mmap                                             DontDelete|Function 1
     nanoseconds                                    functionBunNanoseconds                                              DontDelete|Function 0
     openInEditor                                   BunObject_callback_openInEditor                                     DontDelete|Function 1
+    polyPythonCall                                 BunObject_callback_polyPythonCall                                   DontDelete|Function 1
     origin                                         BunObject_lazyPropCb_wrap_origin                                    DontEnum|ReadOnly|DontDelete|PropertyCallback
     version_with_sha                               constructBunVersionWithSha                                          DontEnum|ReadOnly|DontDelete|PropertyCallback
     password                                       constructPasswordObject                                             DontDelete|PropertyCallback
diff --git a/src/runtime/Cargo.toml b/src/runtime/Cargo.toml
index d1f50e8..6b978f0 100644
--- a/src/runtime/Cargo.toml
+++ b/src/runtime/Cargo.toml
@@ -10,6 +10,7 @@ path = "lib.rs"
 workspace = true
 
 [dependencies]
+poly_python.workspace = true
 bun_opaque.workspace = true
 rust-argon2.workspace = true
 bcrypt.workspace = true
diff --git a/src/runtime/api/BunObject.rs b/src/runtime/api/BunObject.rs
index 58fbdfb..92a7229 100644
--- a/src/runtime/api/BunObject.rs
+++ b/src/runtime/api/BunObject.rs
@@ -290,6 +290,7 @@ pub mod bun_object {
         BunObject_callback_mmap => super::mmap_file,
         BunObject_callback_nanoseconds => super::nanoseconds,
         BunObject_callback_openInEditor => super::open_in_editor,
+        BunObject_callback_polyPythonCall => super::poly_python_call,
         BunObject_callback_registerMacro => super::register_macro,
         BunObject_callback_resolve => super::resolve,
         BunObject_callback_resolveSync => super::resolve_sync,
@@ -388,6 +389,48 @@ fn get_cron_object(global_this: &JSGlobalObject, obj: &JSObject) -> JSValue {
     crate::api::cron::get_cron_object(global_this, obj)
 }
 
+#[bun_jsc::host_fn]
+fn poly_python_call(global_this: &JSGlobalObject, callframe: &CallFrame) -> JsResult<JSValue> {
+    let trace = std::env::var_os("POLY_TRACE").is_some();
+    if trace {
+        eprintln!("[poly trace] entered Bun.polyPythonCall host callback");
+    }
+    let [request_value] = callframe.arguments_as_array::<1>();
+    if callframe.arguments_count() < 1 {
+        return Err(global_this.throw_not_enough_arguments("polyPythonCall", 1, 0));
+    }
+    if !request_value.is_string() {
+        return Err(global_this.throw_invalid_argument_type(
+            "polyPythonCall",
+            "requestJson",
+            "string",
+        ));
+    }
+
+    let request = request_value.to_bun_string(global_this)?;
+    let request_utf8 = request.to_utf8();
+    let request_json = std::str::from_utf8(request_utf8.slice()).map_err(|error| {
+        global_this.throw_invalid_arguments(format_args!(
+            "polyPythonCall request must be valid UTF-8: {error}"
+        ))
+    })?.to_owned();
+    if trace {
+        eprintln!("[poly trace] copied Bun.polyPythonCall request");
+    }
+    let response = poly_python::call_json(&request_json).map_err(|error| {
+        global_this.throw(format_args!("in-process RustPython call failed: {error}"))
+    })?;
+    if trace {
+        eprintln!("[poly trace] RustPython returned to Bun.polyPythonCall");
+    }
+
+    let result = BunString::clone_utf8(response.as_bytes()).to_js(global_this);
+    if trace {
+        eprintln!("[poly trace] encoded Bun.polyPythonCall response");
+    }
+    result
+}
+
 #[bun_jsc::host_fn]
 fn shell_escape(global_this: &JSGlobalObject, callframe: &CallFrame) -> JsResult<JSValue> {
     use bun_jsc::StringJsc as _;
diff --git a/src/runtime/api/bun/spawn/stdio.rs b/src/runtime/api/bun/spawn/stdio.rs
index 62b4c224..a4260b77 100644
--- a/src/runtime/api/bun/spawn/stdio.rs
+++ b/src/runtime/api/bun/spawn/stdio.rs
@@ -464,7 +464,7 @@ impl Stdio {
                 )));
             }
 
-            if file_fd >= i32::MAX as _ {
+            if file_fd >= i32::MAX {
                 let mut formatter = jsc::console_object::Formatter::new(global);
                 // `defer formatter.deinit()` — handled by Drop.
                 return Err(global.throw_invalid_arguments(format_args!(
diff --git a/src/runtime/dns_jsc/options_jsc.rs b/src/runtime/dns_jsc/options_jsc.rs
index 348a66e6..3ea3577b 100644
--- a/src/runtime/dns_jsc/options_jsc.rs
+++ b/src/runtime/dns_jsc/options_jsc.rs
@@ -221,8 +221,8 @@ pub(crate) fn result_to_js(this: &GaiResult, global: &JSGlobalObject) -> JsResul
         b"family",
         // `bun_sys::net::Address` exposes `.family() -> i32`.
         match this.address.family() {
-            f if f == super::netc::AF_INET as _ => JSValue::js_number(4.0),
-            f if f == super::netc::AF_INET6 as _ => JSValue::js_number(6.0),
+            f if f == super::netc::AF_INET as i32 => JSValue::js_number(4.0),
+            f if f == super::netc::AF_INET6 as i32 => JSValue::js_number(6.0),
             _ => JSValue::js_number(0.0),
         },
     );
diff --git a/src/runtime/node/node_fs.rs b/src/runtime/node/node_fs.rs
index b1424354..7b949ce8 100644
--- a/src/runtime/node/node_fs.rs
+++ b/src/runtime/node/node_fs.rs
@@ -1528,7 +1528,7 @@ mod _async_tasks {
             'brk: {
                 match result {
                     Err(ref err) => {
-                        if err.errno == E::EEXIST as _ && !args.flags.error_on_exist {
+                        if err.errno == E::EEXIST as u16 && !args.flags.error_on_exist {
                             break 'brk;
                         }
                         parent.finish_concurrently(result);
@@ -1902,7 +1902,7 @@ mod _async_tasks {
                         &this.args,
                     );
                     if let Err(e) = &r {
-                        if e.errno == E::EEXIST as _ && !args.flags.error_on_exist {
+                        if e.errno == E::EEXIST as u16 && !args.flags.error_on_exist {
                             this.finish_concurrently(Ok(()));
                             return;
                         }
@@ -1941,7 +1941,7 @@ mod _async_tasks {
                         &this.args,
                     );
                     if let Err(e) = &r {
-                        if e.errno == E::EEXIST as _ && !args.flags.error_on_exist {
+                        if e.errno == E::EEXIST as u16 && !args.flags.error_on_exist {
                             this.on_copy(src, dest);
                             this.finish_concurrently(Ok(()));
                             return;
@@ -8391,7 +8391,7 @@ impl NodeFS {
                     args,
                 );
                 if let Err(ref e) = r {
-                    if e.errno == E::EEXIST as _ && !cp_flags.error_on_exist {
+                    if e.errno == E::EEXIST as u16 && !cp_flags.error_on_exist {
                         return Ok(());
                     }
                 }
@@ -8420,7 +8420,7 @@ impl NodeFS {
                     args,
                 );
                 if let Err(ref e) = r {
-                    if e.errno == E::EEXIST as _ && !cp_flags.error_on_exist {
+                    if e.errno == E::EEXIST as u16 && !cp_flags.error_on_exist {
                         return Ok(());
                     }
                 }
@@ -8548,7 +8548,7 @@ impl NodeFS {
                         args,
                     );
                     if let Err(ref e) = r {
-                        if e.errno == E::EEXIST as _ && !cp_flags.error_on_exist {
+                        if e.errno == E::EEXIST as u16 && !cp_flags.error_on_exist {
                             continue;
                         }
                         return r;
diff --git a/scripts/build/bun.ts b/scripts/build/bun.ts
index be8aa5a2..c4bd564e 100644
--- a/scripts/build/bun.ts
+++ b/scripts/build/bun.ts
@@ -65,7 +65,10 @@ function systemLibs(cfg: Config): string[] {
       // (compiler-rt builtins). -llog for __android_log_*.
       libs.push("-lc", "-lm", "-llog");
     } else {
-      libs.push("-lc", "-lpthread", "-ldl");
+      // RustPython's `_ctypes` module uses the system libffi. Cargo records
+      // that native dependency, but Bun links Rust through a static archive,
+      // so the final C++ link must carry the library explicitly.
+      libs.push("-lc", "-lpthread", "-ldl", "-lffi");
       // libatomic: static by default (CI distros ship it), dynamic on Arch-like.
       // The static path needs to be the actual file path for lld to find it;
       // dynamic uses -l syntax. We emit what CMake does: bare libatomic.a gets
@@ -108,6 +111,7 @@ function systemLibs(cfg: Config): string[] {
       "userenv.lib",
       "dbghelp.lib",
       "crypt32.lib",
+      "propsys.lib",
       "wsock32.lib", // ws2_32 + wsock32 — wsock32 has TransmitFile (sendfile equiv)
       "ws2_32.lib",
       "delayimp.lib", // required for /delayload: in release
diff --git a/src/jsc/bindings/windows/rescle.cpp b/src/jsc/bindings/windows/rescle.cpp
index a3171b4e..1eb7b316 100644
--- a/src/jsc/bindings/windows/rescle.cpp
+++ b/src/jsc/bindings/windows/rescle.cpp
@@ -112,7 +112,7 @@ inline T round(T value, int modula = 4)
 std::wstring ReadFileToString(const wchar_t* filename)
 {
     std::wifstream wif(filename);
-    wif.imbue(std::locale(std::locale::empty(), new std::codecvt_utf8<wchar_t>));
+    wif.imbue(std::locale(std::locale::classic(), new std::codecvt_utf8<wchar_t>));
     std::wstringstream wss;
     wss << wif.rdbuf();
     return wss.str();
diff --git a/src/runtime/node/win_watcher.rs b/src/runtime/node/win_watcher.rs
index 897fe5cd..49e7b7a9 100644
--- a/src/runtime/node/win_watcher.rs
+++ b/src/runtime/node/win_watcher.rs
@@ -321,7 +321,7 @@ impl PathWatcher {
         let readlink_result = sys::readlink(path, &mut outbuf);
         let event_path: &ZStr = match readlink_result {
             sys::Result::Err(err) => 'brk: {
-                if err.errno == sys::E::NOENT as _ {
+                if err.errno == sys::E::NOENT as u16 {
                     return sys::Result::Err(sys::Error {
                         errno: err.errno,
                         syscall: sys::Tag::open,
