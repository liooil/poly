diff --git a/Cargo.toml b/Cargo.toml
index 50c3cf7..7900c4f 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -93,6 +93,7 @@ members = [
   "src/options_types",
   "src/output",
   "src/runtime",
+  "src/poly_python",
   "src/simdutf_sys",
   "src/which",
   "src/transpiler",
@@ -453,6 +454,7 @@ bun_options_types = { path = "src/options_types" }
 bun_bunfig = { path = "src/bunfig" }
 bun_output = { path = "src/output" }
 bun_runtime = { path = "src/runtime", default-features = false }
+poly_python = { path = "src/poly_python" }
 bun_simdutf_sys = { path = "src/simdutf_sys" }
 bun_which = { path = "src/which" }
 bun_transpiler = { path = "src/transpiler" }
diff --git a/src/bun_bin/Cargo.toml b/src/bun_bin/Cargo.toml
index 94839e8..91d0ffb 100644
--- a/src/bun_bin/Cargo.toml
+++ b/src/bun_bin/Cargo.toml
@@ -34,4 +34,5 @@ bun_platform.workspace = true
 # Phase-C: bun_runtime now compiles green; pulled in for `Cli::start()`.
 # bun_jsc remains gated until the JSC bridge links.
 bun_runtime.workspace = true
+poly_python.workspace = true
 # bun_jsc.workspace = true
diff --git a/src/bun_bin/lib.rs b/src/bun_bin/lib.rs
index ba12490..d76d844 100644
--- a/src/bun_bin/lib.rs
+++ b/src/bun_bin/lib.rs
@@ -200,6 +200,10 @@ pub(crate) unsafe extern "C" fn main(argc: c_int, argv: *const *const c_char) ->
     output::stdio::init();
     let _flush = output::flush_guard();
 
+    if let Some(exit_code) = run_python_entrypoint() {
+        return exit_code;
+    }
+
     // 5. Per-thread stack-limit cache for the JS recursion guard.
     StackCheck::configure_thread();
     bun_io::ParentDeathWatchdog::install();
@@ -209,3 +213,28 @@ pub(crate) unsafe extern "C" fn main(argc: c_int, argv: *const *const c_char) ->
     // `Global::exit` is `-> !`; it coerces to the `c_int` return type.
     Global::exit(0)
 }
+
+fn run_python_entrypoint() -> Option<c_int> {
+    let mut args = std::env::args_os().skip(1);
+    let first = args.next()?;
+    let (entry, script_args) = if first == "run" {
+        (args.next()?, args)
+    } else {
+        (first, args)
+    };
+    let path = std::path::PathBuf::from(entry);
+    if path.extension().and_then(|extension| extension.to_str()) != Some("py") {
+        return None;
+    }
+
+    let script_args: Vec<String> = script_args
+        .map(|argument| argument.to_string_lossy().into_owned())
+        .collect();
+    Some(match poly_python::run_file(&path, &script_args) {
+        Ok(exit_code) => exit_code as c_int,
+        Err(error) => {
+            eprintln!("poly: {error}");
+            1
+        }
+    })
+}
diff --git a/src/jsc/bindings/BunObject+exports.h b/src/jsc/bindings/BunObject+exports.h
index 47a1f8d..e6da7af 100644
--- a/src/jsc/bindings/BunObject+exports.h
+++ b/src/jsc/bindings/BunObject+exports.h
@@ -65,6 +65,7 @@
     macro(mmap) \
     macro(nanoseconds) \
     macro(openInEditor) \
+    macro(polyPythonCall) \
     macro(registerMacro) \
     macro(resolve) \
     macro(resolveSync) \
diff --git a/src/jsc/bindings/BunObject.cpp b/src/jsc/bindings/BunObject.cpp
index 696460b..61edf18 100644
--- a/src/jsc/bindings/BunObject.cpp
+++ b/src/jsc/bindings/BunObject.cpp
@@ -985,6 +985,7 @@ JSC_DEFINE_HOST_FUNCTION(functionFileURLToPath, (JSC::JSGlobalObject * globalObj
     mmap                                           BunObject_callback_mmap                                             DontDelete|Function 1
     nanoseconds                                    functionBunNanoseconds                                              DontDelete|Function 0
     openInEditor                                   BunObject_callback_openInEditor                                     DontDelete|Function 1
+    polyPythonCall                                 BunObject_callback_polyPythonCall                                   DontDelete|Function 1
     origin                                         BunObject_lazyPropCb_wrap_origin                                    DontEnum|ReadOnly|DontDelete|PropertyCallback
     version_with_sha                               constructBunVersionWithSha                                          DontEnum|ReadOnly|DontDelete|PropertyCallback
     password                                       constructPasswordObject                                             DontDelete|PropertyCallback
diff --git a/src/runtime/Cargo.toml b/src/runtime/Cargo.toml
index d1f50e8..6b978f0 100644
--- a/src/runtime/Cargo.toml
+++ b/src/runtime/Cargo.toml
@@ -10,6 +10,7 @@ path = "lib.rs"
 workspace = true
 
 [dependencies]
+poly_python.workspace = true
 bun_opaque.workspace = true
 rust-argon2.workspace = true
 bcrypt.workspace = true
diff --git a/src/runtime/api/BunObject.rs b/src/runtime/api/BunObject.rs
index 58fbdfb..92a7229 100644
--- a/src/runtime/api/BunObject.rs
+++ b/src/runtime/api/BunObject.rs
@@ -290,6 +290,7 @@ pub mod bun_object {
         BunObject_callback_mmap => super::mmap_file,
         BunObject_callback_nanoseconds => super::nanoseconds,
         BunObject_callback_openInEditor => super::open_in_editor,
+        BunObject_callback_polyPythonCall => super::poly_python_call,
         BunObject_callback_registerMacro => super::register_macro,
         BunObject_callback_resolve => super::resolve,
         BunObject_callback_resolveSync => super::resolve_sync,
@@ -388,6 +389,34 @@ fn get_cron_object(global_this: &JSGlobalObject, obj: &JSObject) -> JSValue {
     crate::api::cron::get_cron_object(global_this, obj)
 }
 
+#[bun_jsc::host_fn]
+fn poly_python_call(global_this: &JSGlobalObject, callframe: &CallFrame) -> JsResult<JSValue> {
+    let [request_value] = callframe.arguments_as_array::<1>();
+    if callframe.arguments_count() < 1 {
+        return Err(global_this.throw_not_enough_arguments("polyPythonCall", 1, 0));
+    }
+    if !request_value.is_string() {
+        return Err(global_this.throw_invalid_argument_type(
+            "polyPythonCall",
+            "requestJson",
+            "string",
+        ));
+    }
+
+    let request = request_value.to_bun_string(global_this)?;
+    let request_utf8 = request.to_utf8();
+    let request_json = std::str::from_utf8(request_utf8.slice()).map_err(|error| {
+        global_this.throw_invalid_arguments(format_args!(
+            "polyPythonCall request must be valid UTF-8: {error}"
+        ))
+    })?;
+    let response = poly_python::call_json(request_json).map_err(|error| {
+        global_this.throw(format_args!("in-process RustPython call failed: {error}"))
+    })?;
+
+    BunString::from_bytes(response.as_bytes()).to_js(global_this)
+}
+
 #[bun_jsc::host_fn]
 fn shell_escape(global_this: &JSGlobalObject, callframe: &CallFrame) -> JsResult<JSValue> {
     use bun_jsc::StringJsc as _;
